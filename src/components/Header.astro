---
import { site, GIFT_CARDS_URL } from "../data/site";

type NavChild = {
  href: string;
  label: string;
  external?: boolean;
};

type NavLink = {
  label: string;
  href?: string;
  external?: boolean;
  children?: NavChild[];
};

const shopLinks: NavChild[] = [
  { href: GIFT_CARDS_URL, label: "Gift Cards", external: true },
];

const navLinks: NavLink[] = [
  { href: "/#about", label: "About" },
  { href: "/#services", label: "Services" },
  { href: "/#team", label: "Team" },
  { label: "Shop", children: shopLinks },
  { href: "/#policies", label: "Policies" },
  { href: "/#connect", label: "Connect" },
];

const mobileNavLinks: NavLink[] = navLinks.flatMap((link) =>
  link.href === "/#connect"
    ? [link, { href: "/#reviews", label: "Reviews" }]
    : [link]
);

const externalLinkAttrs = (external?: boolean) =>
  external ? { target: "_blank", rel: "noopener noreferrer" } : {};

const shopMenuId = "shop-submenu";
---

<header class="site-header">
  <div class="site-header__inner">
    <nav class="site-header__nav site-header__nav--left" aria-label="Primary">
      {
        navLinks.slice(0, 3).map((link) => (
          <a class="site-header__link" href={link.href} {...externalLinkAttrs(link.external)}>
            {link.label}
          </a>
        ))
      }
    </nav>

    <a class="site-header__logo" href="/" aria-label={site.name}>
      <img
        class="site-header__logo-image site-header__logo-image--light"
        src="/images/salon-citrine-logo.png"
        alt=""
        width="1180"
        height="430"
        fetchpriority="high"
        decoding="async"
      />
      <img
        class="site-header__logo-image site-header__logo-image--dark"
        src="/images/salon-citrine-banner-dark.png"
        alt=""
        width="1180"
        height="430"
        loading="lazy"
        decoding="async"
        aria-hidden="true"
      />
    </a>

    <nav class="site-header__nav site-header__nav--right" aria-label="Secondary">
      {
        navLinks.slice(3).map((link) =>
          link.children ? (
            <div class="site-header__dropdown" data-nav-dropdown>
              <button
                class="site-header__link site-header__dropdown-trigger"
                type="button"
                aria-expanded="false"
                aria-haspopup="true"
                aria-controls={shopMenuId}
                data-dropdown-trigger
              >
                {link.label}
                <svg
                  class="site-header__dropdown-chevron"
                  viewBox="0 0 12 12"
                  aria-hidden="true"
                >
                  <path d="M2.5 4.5 6 8l3.5-3.5" fill="none" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"></path>
                </svg>
              </button>
              <ul
                id={shopMenuId}
                class="site-header__dropdown-menu"
                role="menu"
                aria-label="Shop"
                data-dropdown-menu
              >
                {
                  link.children.map((child) => (
                    <li role="none">
                      <a
                        class="site-header__dropdown-item"
                        href={child.href}
                        role="menuitem"
                        aria-label={child.external ? `${child.label} (opens in new tab)` : undefined}
                        {...externalLinkAttrs(child.external)}
                      >
                        {child.label}
                      </a>
                    </li>
                  ))
                }
              </ul>
            </div>
          ) : (
            <a
              class="site-header__link"
              href={link.href}
              {...externalLinkAttrs(link.external)}
            >
              {link.label}
            </a>
          )
        )
      }
    </nav>

    <button
      class="site-header__theme-toggle site-header__theme-toggle--desktop"
      type="button"
      aria-label="Switch to dark mode"
      data-theme-toggle
    >
      <svg
        class="theme-toggle__icon theme-toggle__icon--moon"
        viewBox="0 0 24 24"
        aria-hidden="true"
      >
        <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
      </svg>
      <svg
        class="theme-toggle__icon theme-toggle__icon--sun"
        viewBox="0 0 24 24"
        aria-hidden="true"
      >
        <circle cx="12" cy="12" r="5"></circle>
        <line x1="12" y1="1" x2="12" y2="3"></line>
        <line x1="12" y1="21" x2="12" y2="23"></line>
        <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
        <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
        <line x1="1" y1="12" x2="3" y2="12"></line>
        <line x1="21" y1="12" x2="23" y2="12"></line>
        <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
        <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
      </svg>
    </button>

    <div class="site-header__controls">
      <button
        class="site-header__menu-toggle"
        type="button"
        aria-label="Open menu"
        aria-expanded="false"
        data-menu-toggle
      >
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>

    <nav
      class="site-header__nav site-header__nav--mobile"
      aria-label="Mobile"
      aria-hidden="true"
      data-mobile-nav
    >
      {
        mobileNavLinks.map((link) =>
          link.children ? (
            <div class="site-header__dropdown site-header__dropdown--mobile" data-nav-dropdown>
              <button
                class="site-header__link site-header__dropdown-trigger"
                type="button"
                aria-expanded="false"
                aria-haspopup="true"
                aria-controls={`${shopMenuId}-mobile`}
                data-dropdown-trigger
              >
                {link.label}
                <svg
                  class="site-header__dropdown-chevron"
                  viewBox="0 0 12 12"
                  aria-hidden="true"
                >
                  <path d="M2.5 4.5 6 8l3.5-3.5" fill="none" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"></path>
                </svg>
              </button>
              <ul
                id={`${shopMenuId}-mobile`}
                class="site-header__dropdown-menu"
                role="menu"
                aria-label="Shop"
                data-dropdown-menu
              >
                {
                  link.children.map((child) => (
                    <li role="none">
                      <a
                        class="site-header__dropdown-item"
                        href={child.href}
                        role="menuitem"
                        aria-label={child.external ? `${child.label} (opens in new tab)` : undefined}
                        {...externalLinkAttrs(child.external)}
                      >
                        {child.label}
                      </a>
                    </li>
                  ))
                }
              </ul>
            </div>
          ) : (
            <a
              class="site-header__link"
              href={link.href}
              {...externalLinkAttrs(link.external)}
            >
              {link.label}
            </a>
          )
        )
      }

      <button
        class="site-header__theme-toggle site-header__theme-toggle--mobile"
        type="button"
        aria-label="Switch to dark mode"
        data-theme-toggle
      >
        <svg
          class="theme-toggle__icon theme-toggle__icon--moon"
          viewBox="0 0 24 24"
          aria-hidden="true"
        >
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
        </svg>
        <svg
          class="theme-toggle__icon theme-toggle__icon--sun"
          viewBox="0 0 24 24"
          aria-hidden="true"
        >
          <circle cx="12" cy="12" r="5"></circle>
          <line x1="12" y1="1" x2="12" y2="3"></line>
          <line x1="12" y1="21" x2="12" y2="23"></line>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
          <line x1="1" y1="12" x2="3" y2="12"></line>
          <line x1="21" y1="12" x2="23" y2="12"></line>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
        </svg>
      </button>
      <button
        class="site-header__menu-close"
        type="button"
        aria-label="Close menu"
        data-menu-close
      >
        <span aria-hidden="true">&times;</span>
      </button>
    </nav>
  </div>
</header>
